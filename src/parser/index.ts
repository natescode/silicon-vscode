/**
 * Parser Module
 *
 * Stage 1 of the compilation pipeline: Convert source code into a parse tree.
 *
 * @see parser.ts - Main parsing logic
 * @see ../grammar/silicon-official.ohm - Grammar definitions
 */

export { default as parse } from './parser'
